{
  "name": "@wasp/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types used by /web and /chat-service",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
