{
  "name": "@wasp/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types used by /web and /chat-service",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'shared: types-only package, no build needed'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
