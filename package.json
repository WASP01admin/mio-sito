{
  "name": "wasp-website",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "web",
    "chat-service",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -w shared && npm run build -w chat-service && npm run build -w web"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "csv-parse": "^7.0.1",
    "csv-stringify": "^6.8.1",
    "dotenv": "^17.4.2",
    "xlsx": "^0.18.5"
  }
}
