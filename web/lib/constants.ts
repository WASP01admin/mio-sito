export const ADMIN_CONTACT_EMAIL = "info@waspnest.org";

// The association code used for members who apply directly to WASP (no
// existing association vouching for them). Never auto-notify this "self"
// association -- it would just be WASP emailing WASP.
export const WASP_DIRECT_ASSOCIATION_CODE = "ITAWASP";

// Honorary members, created directly by an admin. No verification email,
// no payment, no association check -- the card is active immediately.
export const WASP_VIP_ASSOCIATION_CODE = "ITAVIP0";
